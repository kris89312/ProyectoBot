// Tailwind v4 no necesita este archivo para la configuración de tema.
// Todo está en globals.css dentro del bloque @theme.
// Este archivo puede estar vacío o eliminarse.
import type { Config } from "tailwindcss";
const config: Config = {};
export default config;
